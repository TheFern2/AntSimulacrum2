[package]
name = "ant_simulacrum"
version = "0.1.0"
edition = "2024"

[dependencies]
macroquad = "0.4"
rand = "0.8"

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
tray-item = "0.7"
dirs = "5"
